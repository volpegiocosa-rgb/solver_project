function logger(event, state, opts)
% LOGGER  Log deterministico e riproducibile (rif. CLAUDE.md §8).
%   Registra: seed, opts, storia sigma, violazione PER-VINCOLO, best-feasible, n_eval.
%   Stesso input + stesso seed => stesso output. Output in /results.
% STATO: STUB. Fase 1.
    error('logger:notImplemented', 'STUB: vedi CLAUDE.md §8.');
end
