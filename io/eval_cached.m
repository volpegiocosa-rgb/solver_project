function [f, cineq, ceq, cache] = eval_cached(fun, x, cache)
% EVAL_CACHED  Valutazione con cache (rif. CLAUDE.md §4, §3.1).
%   Evita di ripetere la simulazione tra obiettivo e repair sullo stesso x.
%   Firma utente attesa: [f, cineq, ceq] = fun(x, other).
% STATO: STUB. Fase 1-2.
    error('eval_cached:notImplemented', 'STUB: vedi CLAUDE.md §4.');
end
